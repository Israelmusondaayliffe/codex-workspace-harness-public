# Workspace CLAUDE.md

The workspace contract lives in `AGENTS.md` at this root; read it and follow it as the contract for this workspace. It covers the working method, file layout, context loading, output convention (`OUTPUTS/YYYY-MM-DD/`, `projectname_type_v1.ext`), safety boundaries, and verification. Project contracts under `PROJECTS/<slug>/` add narrower instructions; the closest contract wins.
