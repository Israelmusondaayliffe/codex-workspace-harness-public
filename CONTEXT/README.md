# Private context

This folder holds operator-specific context that improves planning and execution. The completed context files are ignored by Git so a public harness can remain reusable without publishing private information.

Copy each `*.template.md` file to the same name without `.template`, fill it in, and keep the completed file local. Store only operationally useful context. Do not turn this folder into a personal archive or place credentials here.
